import playsound
# This is how we can import a module in our code
''' Modules are of two type: 
                            1. Build in modules --> which is pre installed in vs code
                            2. Externel modules --> which we have to download from terminal
    We can install modules using command pip install moduleName
    Type this in terminal
'''